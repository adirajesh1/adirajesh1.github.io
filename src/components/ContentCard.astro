---
interface Props {
  href: string;
  title: string;
  description: string;
  date?: Date;
  tags?: string[];
}

const { href, title, description, date, tags = [] } = Astro.props;
const formattedDate = date?.toLocaleDateString("en-US", {
  month: "long",
  day: "numeric",
  year: "numeric",
});
---

<article class="entry">
  <h3><a href={href}>{title}</a></h3>
  {(formattedDate || tags.length > 0) && (
    <div class="meta">
      {formattedDate && (
        <span>Published: <time datetime={date?.toISOString()}>{formattedDate}</time></span>
      )}
      {tags.length > 0 && <span>{tags.join(", ")}</span>}
    </div>
  )}
  <p>{description}</p>
</article>
